# 📊 Hourly Training System Architecture

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                     GITHUB ACTIONS WORKFLOW                          │
│                   (Runs Every Hour 9 AM - 8 PM)                     │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────┐
                    │   DETERMINE MODE          │
                    │   - Check current hour    │
                    │   - Set timeframe         │
                    │   - Choose: hourly/full   │
                    └───────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
        ┌───────────────────────┐       ┌───────────────────────┐
        │   HOURLY MODE         │       │   FULL MODE           │
        │   (9 AM - 7 PM)       │       │   (8 PM)              │
        │                       │       │                       │
        │   • 1h interval       │       │   • 2y period         │
        │   • Last 2 hours      │       │   • Daily data        │
        │   • 5 epochs          │       │   • 100 epochs        │
        │   • 5-10 min          │       │   • 30-60 min         │
        └───────────────────────┘       └───────────────────────┘
                    │                               │
                    └───────────────┬───────────────┘
                                    ▼
                    ┌───────────────────────────┐
                    │   FETCH DATA              │
                    │   - yFinance API          │
                    │   - Stocks (10)           │
                    │   - Forex (5)             │
                    │   - Add metadata          │
                    └───────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────┐
                    │   STORE IN DATABASE       │
                    │   - SQLite (training.db)  │
                    │   - market_data table     │
                    │   - training_runs table   │
                    │   - Timeframe tracking    │
                    └───────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────┐
                    │   PARALLEL TRAINING       │
                    │   ┌─────────────────────┐ │
                    │   │ Stock Models (10)   │ │
                    │   │ - AAPL, MSFT, etc   │ │
                    │   └─────────────────────┘ │
                    │   ┌─────────────────────┐ │
                    │   │ Forex Models (5)    │ │
                    │   │ - EURUSD, GBPUSD    │ │
                    │   └─────────────────────┘ │
                    └───────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────┐
                    │   EVALUATE MODELS         │
                    │   - Calculate accuracy    │
                    │   - Measure loss          │
                    │   - Generate metrics      │
                    │   - Create summary        │
                    └───────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────┐
                    │   SAVE ARTIFACTS          │
                    │   - Trained models (.pt)  │
                    │   - Database (SQLite)     │
                    │   - CSV data files        │
                    │   - Evaluation results    │
                    └───────────────────────────┘
                                    │
                                    ▼
                    ┌───────────────────────────┐
                    │   DEPLOY (Full Mode Only) │
                    │   - Commit models         │
                    │   - Push to repository    │
                    │   - Update database       │
                    └───────────────────────────┘
```

## Daily Timeline

```
UTC Time    Mode      Action                              Duration
─────────────────────────────────────────────────────────────────────
00:00-08:59  IDLE     Waiting for next scheduled run      -
09:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
10:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
11:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
12:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
13:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
14:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
15:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
16:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
17:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
18:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
19:00        HOURLY   Fetch 1h data → Train → Save        5-10 min
20:00        FULL     Fetch 2y data → Train → Deploy      30-60 min
21:00-23:59  IDLE     Waiting for next day                -
```

## Data Flow

```
┌──────────────┐
│  yFinance    │  ← Fetch market data
│  API         │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  CSV Files   │  ← Temporary storage
│  (Artifacts) │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  SQLite Database (training_data.db)                  │
│  ┌────────────────────────────────────────────────┐  │
│  │  market_data                                   │  │
│  │  - symbol, date, OHLCV                        │  │
│  │  - timeframe, interval, hour                  │  │
│  │  - Indexed by symbol+date, timeframe, hour    │  │
│  └────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │  training_runs                                 │  │
│  │  - run_date, mode, timeframe, hour            │  │
│  │  - symbols_count, rows_processed, status      │  │
│  └────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │  model_metadata                                │  │
│  │  - symbol, training_date, accuracy, loss      │  │
│  │  - timeframe, training_mode, hour, epochs     │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│  ML Models   │  ← Trained PyTorch models
│  (.pt files) │
└──────────────┘
```

## Training Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│  SINGLE MODEL TRAINING PIPELINE                             │
└─────────────────────────────────────────────────────────────┘

1. LOAD DATA FROM DATABASE
   ┌────────────────────────────────────────┐
   │  Query: SELECT * FROM market_data      │
   │  WHERE symbol = 'AAPL'                 │
   │  AND interval = '1h'                   │
   │  AND date >= datetime('now', '-30d')   │
   └────────────────────────────────────────┘
                    │
                    ▼
2. PREPARE FEATURES
   ┌────────────────────────────────────────┐
   │  • Calculate technical indicators      │
   │  • Normalize data                      │
   │  • Create feature matrix               │
   │  • Add timeframe context               │
   └────────────────────────────────────────┘
                    │
                    ▼
3. TRAIN MODEL
   ┌────────────────────────────────────────┐
   │  • Load existing model (if incremental)│
   │  • Train for N epochs                  │
   │  • Track loss and accuracy             │
   │  • Save best model                     │
   └────────────────────────────────────────┘
                    │
                    ▼
4. SAVE METADATA
   ┌────────────────────────────────────────┐
   │  INSERT INTO model_metadata            │
   │  (symbol, timeframe, accuracy, loss,   │
   │   training_mode, hour, epochs)         │
   └────────────────────────────────────────┘
```

## Parallel Execution

```
┌─────────────────────────────────────────────────────────────┐
│  PARALLEL TRAINING (Matrix Strategy)                        │
└─────────────────────────────────────────────────────────────┘

Stock Models (Parallel)          Forex Models (Parallel)
┌──────────┐                     ┌──────────┐
│  AAPL    │ ─┐                  │  EURUSD  │ ─┐
└──────────┘  │                  └──────────┘  │
┌──────────┐  │                  ┌──────────┐  │
│  MSFT    │ ─┤                  │  GBPUSD  │ ─┤
└──────────┘  │                  └──────────┘  │
┌──────────┐  │                  ┌──────────┐  │
│  GOOGL   │ ─┤  All run         │  USDJPY  │ ─┤  All run
└──────────┘  │  simultaneously  └──────────┘  │  simultaneously
┌──────────┐  │                  ┌──────────┐  │
│  AMZN    │ ─┤                  │  AUDUSD  │ ─┤
└──────────┘  │                  └──────────┘  │
┌──────────┐  │                  ┌──────────┐  │
│  TSLA    │ ─┤                  │  USDCAD  │ ─┤
└──────────┘  │                  └──────────┘  │
     ...      │                       
└──────────────┘                  └──────────────┘

Total: 10 stock + 5 forex = 15 models training in parallel
```

## Artifact Management

```
┌─────────────────────────────────────────────────────────────┐
│  ARTIFACT LIFECYCLE                                          │
└─────────────────────────────────────────────────────────────┘

After Each Run:
┌────────────────────────────────────────────────────────────┐
│  Upload Artifacts                                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  market-data-{run_number}     (7 days retention)    │  │
│  │  - stocks_YYYYMMDD_HHMM.csv                         │  │
│  │  - forex_YYYYMMDD_HHMM.csv                          │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  model-{symbol}-{run_number}  (90 days retention)   │  │
│  │  - AAPL_model.pt                                     │  │
│  │  - MSFT_model.pt                                     │  │
│  │  - ... (15 models total)                             │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  training-database            (90 days retention)    │  │
│  │  - training_data.db (persistent across runs)        │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  evaluation-results-{run_number} (90 days)          │  │
│  │  - evaluation_results.json                           │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘

Full Mode Only:
┌────────────────────────────────────────────────────────────┐
│  Commit to Repository                                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  models/*.pt              (All trained models)       │  │
│  │  training_data.db         (Complete database)        │  │
│  │  evaluation_results.json  (Performance metrics)      │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

## Database Schema

```sql
-- Market data with timeframe tracking
CREATE TABLE market_data (
    id INTEGER PRIMARY KEY,
    symbol TEXT NOT NULL,
    asset_type TEXT NOT NULL,
    date TIMESTAMP NOT NULL,
    open REAL,
    high REAL,
    low REAL,
    close REAL,
    volume INTEGER,
    fetch_date TIMESTAMP,
    timeframe TEXT,              -- '1h' or '2y'
    interval TEXT,               -- '1h' or '1d'
    hour INTEGER,                -- 0-23 UTC
    UNIQUE(symbol, date, interval)
);

-- Training run history
CREATE TABLE training_runs (
    id INTEGER PRIMARY KEY,
    run_date TIMESTAMP NOT NULL,
    mode TEXT NOT NULL,          -- 'hourly' or 'full'
    timeframe TEXT,              -- '1h' or '2y'
    hour INTEGER,                -- UTC hour
    symbols_count INTEGER,
    rows_processed INTEGER,
    status TEXT
);

-- Model performance tracking
CREATE TABLE model_metadata (
    id INTEGER PRIMARY KEY,
    symbol TEXT NOT NULL,
    model_type TEXT NOT NULL,
    training_date TIMESTAMP NOT NULL,
    accuracy REAL,
    loss REAL,
    epochs INTEGER,
    model_path TEXT,
    timeframe TEXT,              -- Training timeframe
    training_mode TEXT,          -- 'hourly' or 'full'
    hour INTEGER                 -- Training hour
);
```

## Query Examples

```sql
-- Get latest model for each symbol
SELECT symbol, MAX(training_date) as last_trained, 
       accuracy, timeframe, training_mode
FROM model_metadata
GROUP BY symbol
ORDER BY last_trained DESC;

-- Compare hourly vs full training performance
SELECT 
    symbol,
    training_mode,
    AVG(accuracy) as avg_accuracy,
    AVG(loss) as avg_loss,
    COUNT(*) as training_count
FROM model_metadata
GROUP BY symbol, training_mode;

-- Get hourly data for last 24 hours
SELECT * FROM market_data
WHERE interval = '1h'
AND date >= datetime('now', '-24 hours')
ORDER BY date DESC;

-- Training runs by hour
SELECT 
    hour,
    COUNT(*) as run_count,
    AVG(rows_processed) as avg_rows
FROM training_runs
WHERE mode = 'hourly'
GROUP BY hour
ORDER BY hour;
```

## System Requirements

### GitHub Actions
- ✅ Free tier sufficient
- ✅ ~2 hours compute time per day
- ✅ Artifact storage: ~500 MB

### Dependencies
- ✅ Python 3.11
- ✅ PyTorch
- ✅ yFinance
- ✅ pandas, numpy
- ✅ SQLite (built-in)

### Network
- ✅ yFinance API access
- ✅ GitHub API access
- ✅ No authentication required

## Monitoring Dashboard

```
┌─────────────────────────────────────────────────────────────┐
│  MONITORING DASHBOARD (query_training_data.py)              │
└─────────────────────────────────────────────────────────────┘

Commands:
  runs          Show recent training runs
  performance   Show model performance by timeframe
  hourly        Show hourly data for a symbol
  by-hour       Show training stats by hour of day
  stats         Show overall database statistics
  export        Export table to CSV

Example Output:
┌────────────────────────────────────────────────────────────┐
│  RECENT TRAINING RUNS                                      │
├────────────┬────────┬──────────┬──────┬─────────┬─────────┤
│ run_date   │ mode   │timeframe │ hour │ symbols │ rows    │
├────────────┼────────┼──────────┼──────┼─────────┼─────────┤
│ 2025-12-11 │ hourly │ 1h       │ 14   │ 15      │ 30      │
│ 2025-12-11 │ hourly │ 1h       │ 13   │ 15      │ 30      │
│ 2025-12-11 │ hourly │ 1h       │ 12   │ 15      │ 30      │
│ 2025-12-10 │ full   │ 2y       │ 20   │ 15      │ 10950   │
└────────────┴────────┴──────────┴──────┴─────────┴─────────┘
```

---

**This system provides continuous, automated model training with full timeframe awareness and comprehensive tracking!**
