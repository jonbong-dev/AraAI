# Ara.AI v8 runtime. No torch, no accelerate, no GPU stack: the model is a
# gradient-boosted tree ensemble. Installing this on a CI runner takes seconds.
# The frozen v7 transformer's dependencies live in legacy/requirements.txt.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# Market data + publishing
yfinance>=0.2.0
huggingface_hub>=0.19.0
python-dotenv>=1.0.0

# Tests
pytest>=7.0
